+++
title = "[Rust] serde: JSON 2"
date = 2023-08-19T00:00:00+00:00
[taxonomies]
tags = ["Rust"]
[extra]
naver_log_no = 223187546235
naver_category_no = 29
+++

[https://blog.naver.com/sssang97/221864535670](https://blog.naver.com/sssang97/221864535670)  
이어서 작성한다.  

```javascript
[package]
name = "just_test"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = {version="1.0.183", features=["derive"]}
serde_json = "1"
```

<br>

<br>

<br>

## 역직렬화(Deserialize)

serde에서는 기본적으로, 아래와 같은 형태로 구조체 스키마를 정의해서 역직렬화를 한다.  

![](/images/img_cb819f20a17b0410.png)

![](/images/img_8c22ab009f2c44c6.png)
별다른 문제는 없다.  

근데 json 필드의 컨벤션이 rust의 식별자 컨벤션과 사뭇 다른 경우가 있을 수 있다.  

![](/images/img_819975aa0960e811.png)

![](/images/img_f8f719cb6951e116.png)
이럴때는 rename 속성으로 명시를 해주는게 깔끔한 방법이다.  

<br>

<br>

<br>

## 다형적인 타입에 대한 역직렬화

rust는 enum이라는 훌륭한 union 타입을 제공하기 때문에, 다형적인 타입에 대해서도 대응을 꽤나 유연하게 할 수 있다.  
그래서 다음과 같은 형태로, 특정 json 필드의 타입이 이랬다 저랬다 바꾸는 것도 어렵지 않게 역직렬화를 할 수 있다.  

![](/images/img_ad576446863a6458.png)

방법은 그냥 enum으로 각 variant들을 정의해서 쑤셔넣는 것이다.  

![](/images/img_0b183b0c99dd680d.png)
이러면 배열일 때와 단독 오브젝트일 때가 전부 커버된다.  

<br>

![](/images/img_8e10e4cc42a17ec4.png)
잘 돌아간다.  

예제 코드다.  

```javascript
fn main() {
    #[derive(serde::Deserialize, Debug)]
    struct TestItem {
        item_id: u32,
    }

    #[derive(serde::Deserialize, Debug)]
    #[serde(untagged)]
    enum TestResponseVariant {
        Object(TestItem),
        Array(Vec<TestItem>),
    }

    #[derive(serde::Deserialize, Debug)]
    struct TestResponse {
        id: u32,
        variant: TestResponseVariant,
    }

    {
        let json_raw = r#"{"id":12, "variant": { "item_id": 1234 }}"#;
        let response: TestResponse = serde_json::from_str(json_raw).unwrap();

        println!("{:?}", response);
    }

    {
        let json_raw = r#"{"id":12, "variant": [{ "item_id": 1234 }]}"#;
        let response: TestResponse = serde_json::from_str(json_raw).unwrap();

        println!("{:?}", response);
    }
}
```

이외에도 다양한 기능들을 많이 제공하는 편인데, 더 자세한 것은 serde 문서를 참조하면 좋겠다.  
[https://serde.rs/](https://serde.rs/)  

<br>

<br>

참조  
[https://serde.rs/](https://serde.rs/)
