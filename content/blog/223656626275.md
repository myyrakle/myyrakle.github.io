+++
title = "[Linux] 가상 디스크 만들기"
date = 2024-11-12T00:00:00+00:00
[taxonomies]
tags = ["Linux"]
[extra]
naver_log_no = 223656626275
naver_category_no = 174
+++

Linux에서 모든 것은 파일이다.  
그래서 디스크 정보 또한 파일로 표현되는데, 이 덕분에 가상 디스크를 만들어서 디스크가 있는 것처럼 흉내내는 것도 어렵지 않다.  

우선 블럭 파일을 적당히 만들어준다. 100기가짜리다.  

```javascript
sudo dd if=/dev/zero of=/media/minio.img bs=1M count=102400
```

![](/images/img_62034dbf4b29fb6a.png)

그리고 그걸 포맷한다.  

```javascript
sudo mkfs -t ext4 /media/minio.img
```

![](/images/img_802e8f6469ac28e9.png)
여기선 포맷 타입을 ext4로 했는데, vfat 같은 걸로도 된다.  

디스크를 마운트할 디렉터리를 만들고, mount 명령으로 마운트해주면 끝이다.  

```javascript
sudo mkdir -p /mnt/data/minio
sudo mount -t auto -o loop /media/minio.img /mnt/data/minio/
```

![](/images/img_b16f1c6d00ffd10e.png)
그럼 마운트 경로로 접근해서 디렉터리에 파일을 쓸 수도 있고  

<br>

![](/images/img_c960ae849fef9758.png)
마운트 경로로도 잘 잡힌다.  

<br>

<br>

<br>

## 마운트 해제

umount를 사용하면 마운트를 다시 해제할 수 있다.  

```javascript
sudo umount /mnt/data/minio/
```

![](/images/img_95a7902cdd85b8e0.png)

<br>

<br>

<br>

## 마운트 영구 보존

안타깝게도 mount 명령은 휘발성을 갖고 있다. 재부팅하면 날라간다는 것이다.  

그래서 재부팅 후에도 마운트가 유지되게 하려면 설정파일을 건드려서, 항목을 추가해줘야 한다.  

```javascript
sudo vi /etc/fstab
```

```javascript
/media/minio.img  /mnt/data/minio/  ext4    defaults        0  0
```

<br>

참조  
[https://www.tecmint.com/create-virtual-harddisk-volume-in-linux/](https://www.tecmint.com/create-virtual-harddisk-volume-in-linux/)
