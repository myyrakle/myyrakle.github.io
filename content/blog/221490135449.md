+++
title = "[Rust] 파일 입출력"
date = 2019-03-17T00:00:00+00:00
[taxonomies]
tags = ["Rust"]
[extra]
naver_log_no = 221490135449
naver_category_no = 29
+++

파일 입출력 모듈은 std::fs다.  

일단 텍스트를 하나 써보자  

```javascript
use std::fs;

fn main()
{
    fs::write("text.txt", "안녕하시오")
        .expect("쓰기 실패");
    println!("쓰기 성공");
}
```

![](/images/img_a6f483bd1dc2519a.jpg)

![](/images/img_06b8a773306d032e.jpg)
잘 된다.  

그럼 이걸 다시 수정해서 읽어보자  
read는 읽은걸 Result로 덮어서 반환한다.  

```javascript
use std::fs;

fn main()
{
    let input = fs::read("text.txt").expect("읽기 실패");
    let input = String::from_utf8(input).expect("변환 실패");

    println!("읽기 성공: {}", input);
}
```

![](/images/img_1ea01a4cc8f85049.jpg)

<br>

파일 삭제함수도 있다. 방금 만들었던 텍스트 파일을 지워본다.  

```javascript
use std::fs;

fn main()
{
    fs::remove_file("text.txt").expect("삭제 실패");

    println!("삭제 성공");
}
```

![](/images/img_aa4e5cafb29c9e4a.jpg)

그리고 특정 디렉터리의 목록을 불러오는 놈도 있다.  

```javascript
use std::fs;

fn main()
{
    let dir = fs::read_dir("C:/Program Files").expect("읽기 실패");

    for entry in dir {
        let filename = entry.unwrap().file_name();
        println!("{}", filename.to_str().unwrap());
    };
}
```

![](/images/img_a1602c7c054bd992.jpg)

파일 정보를 확인하는 기능도 있다.  

```javascript
use std::fs;

fn main()
{
    let data
        = fs::metadata("C:/Program Files/Java/jdk-10.0.1/bin/java.exe")
            .expect("확인 실패");

    if data.is_dir() { println!("이것은 디렉터리요"); }
    else { println!("이것은 디렉터리가 아니요."); }

    if data.is_file() { println!("이것은 파일이요"); }
    else { println!("이것은 파일이 아니요"); }

    println!("파일의 크기는 {}바이트요", data.len());
}
```

<br>

![](/images/img_b49c5be3f8b3c725.jpg)

<br>

이외에도 rename, copy 등 편리한 기능들이 많다. 잘 찾아보길 바란다.  
[https://doc.rust-lang.org/std/fs/index.html](https://doc.rust-lang.org/std/fs/index.html)
