+++
title = "[k8s] CustomResourceDefinitions"
date = 2023-05-29T00:00:00+00:00
[taxonomies]
tags = ["Kubernetes"]
[extra]
naver_log_no = 223114653465
naver_category_no = 349
+++

CustomResourceDefinitions는 새로운 종류의 kubernetes 리소스를 정의할 수 있게 해주는 부가기능이다.  
deployment나 cronjob 같은 리소스 단위를 직접 추가하는 것이다.  

나는 여기서 crontab이라는 리소스 단위를 추가해보려 한다.  

![](/images/img_ef7fee3c97faf22b.png)
cronjob은 있지만 crontab이라는 리소스는 기본적으로 존재하지 않는다.  

<br>

<br>

<br>

## CustomResourceDefinitions 정의

다음과 같이 yaml 구성을 정의한다.  

```javascript
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  # name must match the spec fields below, and be in the form: <plural>.<group>
  name: crontabs.stable.example.com
spec:
  # group name to use for REST API: /apis/<group>/<version>
  group: stable.example.com
  # list of versions supported by this CustomResourceDefinition
  versions:
    - name: v1
      # Each version can be enabled/disabled by Served flag.
      served: true
      # One and only one version must be marked as the storage version.
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                cronSpec:
                  type: string
                image:
                  type: string
                replicas:
                  type: integer
  # either Namespaced or Cluster
  scope: Namespaced
  names:
    # plural name to be used in the URL: /apis/<group>/<version>/<plural>
    plural: crontabs
    # singular name to be used as an alias on the CLI and for display
    singular: crontab
    # kind is normally the CamelCased singular type. Your resource manifests use this.
    kind: CronTab
    # shortNames allow shorter string to match your resource on the CLI
    shortNames:
    - ct
```

스키마 부분에서 추후 crontab 리소스를 yaml로 정의할때 어떤 값을 받을 것인지를 정의하고  

![](/images/img_02e66b968458ad33.png)

아래에서는 리소스 이름을 어떻게 지을지를 정한다.  

![](/images/img_290d78eb4db09c35.png)
이름이 crontab이니 kubect get crontab으로 리소스를 조회할 수 있고,  
shortName이 ct니까 kubectl get ct로도 crontab 리소스를 조회할 수 있다는 뜻이 된다.  

저렇게 해서 apply로 띄우면  

![](/images/img_f8630035be36c5c6.png)

이렇게 추가가 된다.  

![](/images/img_54899de31c81422f.png)
방금 내가 만든것 말고도 뭐가 많은 것을 볼 수 있다.  

<br>

<br>

<br>

## 리소스 생성하기

이번에는 방금 정의한 crontab으로 리소스를 만들어보자.  
아까 스키마에 정의했던대로 yaml을 짜넣고  

![](/images/img_812c37c4b17d10cc.png)

apply를 먹여주면 된다.  

![](/images/img_91ad43213bba94c8.png)
별로 다를 것은 없다.  

하지만 지금 여기에는 Pod에 대한 생성이나 제어같은건 설정되어있지 않기 때문에, 뭔가를 더 할수는 없다.  
지금으로서는 그냥 껍데기일 뿐이다.  

여기서 이 리소스에 생명을 불어넣어주는 것이 operator다. operator는 이 custom resource에 대한 생성이나 수정 등을 모니터링하면서 실질적인 동작을 책임진다.  

실질적인 구현은 별도 포스트에서 이어서 해본다.  
[https://blog.naver.com/sssang97/223461756266](https://blog.naver.com/sssang97/223461756266)  

<br>

<br>

참조  
[https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definitions/](https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definitions/)
