+++
title = "[Rust] Github Action: Unit test 스크립트"
date = 2024-03-16T00:00:00+00:00
[taxonomies]
tags = ["Rust"]
[extra]
naver_log_no = 223384944497
naver_category_no = 29
+++

```javascript
name: Cargo Build & Test

on:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build_and_test:
    name: Rust project - latest
    runs-on: ubuntu-latest
    strategy:
      matrix:
        toolchain:
          - stable
    steps:
      - uses: actions/checkout@v3

      - uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('Cargo.toml') }}

      - run: rustup update ${{ matrix.toolchain }} && rustup default ${{ matrix.toolchain }}

      - run: cargo build --verbose
      - run: cargo test --verbose
```
PR에 반응해서 전체 컴파일, 전체 테스트를 돌리는 워크플로 스크립트다.  
그리고 빌드캐시 세팅을 해놔서 toml 변경 이전까지는 증분 빌드가 될 것이다.  

저대로 밀어넣고 돌리면  

![](/images/img_30d7ccad5159c031.png)
테스트를 열심히 돌린다.  

그리고 러스트는 컴파일이 느린 편에 속하기 때문에 빌드캐시가 더욱 중요하다.  
내 경우에는 toml을 기준 키로 캐시를 넣어놨다.  

![](/images/img_8020a7fbbec75957.png)
내 경우에는 빌드 캐시 이전에는 1분이 걸렸고  

<br>

![](/images/img_af26c9d9b449c2cf.png)
빌드캐시를 넣고는 20초가 안 걸렸다. 캐시만 타면 웬만해서 1분 내에 처리할 수 있을 것이다.
