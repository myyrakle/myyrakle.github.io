+++
title = "[Rust] Cargo 확장 커맨드 만들기"
date = 2020-12-07T00:00:00+00:00
[taxonomies]
tags = ["Rust"]
[extra]
naver_log_no = 222165609803
naver_category_no = 29
+++

cargo는 cargo 'asdf'의 형태를 가지는 명령어의 확장을 지원한다.  
덕분에 cargo-edit 등의 유용한 보조도구들이 탄생했고, 계속해서 만들어지고 있는 중이다.  

만드는 법은 의외로 간단하다.  
일단 atty와 structopt라는 크레이트를 추가하고  

```javascript
[dependencies]
atty = { version = "0.2.14", optional = true }
structopt = { version = "0.3.18", optional = true }
```

각 기능마다 동작할 main 함수를 만들어준다.  

![](/images/img_a85a79aca9b4fc3a.png)

이건 foo라는 명령으로 실행될 로직이다.  
그냥 텍스트만 하나 만들어주는거다.  

![](/images/img_17a3198032817c7c.png)

이것도 마찬가지다.  

![](/images/img_f987116c4e1ac177.png)

다 짰으면 Cargo.toml에다가 각 기능에 대한 진입점을 정의한다.  
[[bin]] 섹션에 두면 된다.  

```javascript
[[bin]]
name = "cargo-foo"
path = "src/foo.rs"
required-features = ["foo"]

[[bin]]
name = "cargo-bar"
path = "src/bar.rs"
required-features = ["bar"]
```
이름은 그냥 고유한 이름인 것 같고, path가 그 진입점 main 함수.  
required-features는 기능 이름이다.  

잘 정의했으면 이제 아래 features 섹션에다가 바로 연결해주면 된다.  
이런 식으로  

```javascript
[features]
default = ["foo", "bar"]
foo = ["cli"]
bar = ["cli"]
cli = ["atty", "structopt"]
```
그럼 다 끝난거다.  

이름도 잘 지어놓고, 배포설정 알아서 잘 한 다음에  

![](/images/img_8670200a755b98b0.png)

배포하면 된다.  

![](/images/img_d180292bf62a4b50.png)

잘 올라갔다면 그냥 아래와 같은 식으로 설치가 가능할 것이다.  

![](/images/img_e2993d0bfeb62703.png)

그럼 기대한대로 잘 동작할 것이다.  

![](/images/img_17892da7430d6794.png)

그렇다.  

<br>

[https://github.com/myyrakle/cargo-foobar](https://github.com/myyrakle/cargo-foobar)
