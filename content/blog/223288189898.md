+++
title = "Rupring v0.2.0"
date = 2023-12-10T00:00:00+00:00
[taxonomies]
tags = ["라이브러리"]
[extra]
naver_log_no = 223288189898
naver_category_no = 122
+++

[https://crates.io/crates/rupring](https://crates.io/crates/rupring)  
[https://github.com/myyrakle/rupring/releases/tag/v0.2.0](https://github.com/myyrakle/rupring/releases/tag/v0.2.0)  
만들었다.  
내 취향대로 꾸며본 서버 프레임워크다.  

tokio/hyper 스택으로 직접 쌓았고, Nest.js 스타일의 구성을 최대한 따르고자 했다.  

사용법은 간단하다.  
디펜던시 추가하고  

![](/images/img_8276f2c50036d3e4.png)

이런 형태로 모듈/컨트롤러/route 구조를 짜고  

![](/images/img_5fff9ba0ede547f9.png)

```javascript
#[derive(Debug, Clone, Copy)]
#[rupring::Module(controllers=[HomeController{}], modules=[])]
pub struct RootModule {}

#[derive(Debug, Clone)]
#[rupring::Controller(prefix=/, routes=[hello, echo])]
pub struct HomeController {}

#[rupring::Get(path = /)]
pub fn hello(_request: rupring::Request) -> rupring::Response {
    rupring::Response {
        status: 200,
        body: "Hello, World!".to_string(),
        headers: Default::default(),
    }
}

#[rupring::Get(path = /echo)]
pub fn echo(request: rupring::Request) -> rupring::Response {
    rupring::Response {
        status: 200,
        body: request.body,
        headers: Default::default(),
    }
}

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
    use std::net::SocketAddr;

    let addr = SocketAddr::from(([127, 0, 0, 1], 3000));

    let module = RootModule {};
    rupring::boot::run_server(addr, module).await?;

    Ok(())
}
```

실행하면 된다.  

![](/images/img_cc875f7311bbd8d2.png)

그럼 잘 뜰 것이다.  

![](/images/img_3288e7fd6eeb08a1.png)

![](/images/img_d9e2e73badf91a95.png)

그런대로 동작은 하지만 아직 부족한게 너무 많다.  

직렬화/역직렬화도 없고, 아직 DI도 없다. 미들웨어도 없고, 자동 문서화도 없다.  
틀은 대충 짰으니 이제 천천히 추가해보려 한다.
